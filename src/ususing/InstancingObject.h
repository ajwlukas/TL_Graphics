#pragma once

#include "Instants.h"

/// <summary>
/// 일반적으로 여러개 그릴 오브젝트들,
/// 메쉬와 머터리얼을 공통적으로 꽂아주고,
/// </summary>

//todo : InstancingObject랑 Instants랑 뭔가 역할이 모호하다?, 하나로 합쳐?
class InstancingObject
{
public:
	//InstancingObject();
	//virtual ~InstancingObject();

	//virtual void Update();
	//virtual void Render();

	//void SetPipeLine();
	//void Draw();

	//string name;

	//Mesh* mesh;
	//Material* material;
	//
	//Transform transform;

	//Animator* animator;
	//Skinner* skinner;

	////Instance* CreateInstance() { return instants->CreateInstance(); }//todo : 여기서 붙여주자 component
	//Instance* CreateInstance();
	//void DeleteInstance(const Transform* t);
	//void DeleteInstance(const UINT id) { instants->DeleteInstance(id); }
	//void SetCurrentAnimation(const UINT id, string clipName) { instants->SetCurrentAnimation(id, clipName); }
	//UINT GetCorrespondingInstanceNum(const Transform* t) { return instants->GetCorrespondingInstanceNum(t); }

	//Instants* instants;

	//void ClearInstants() { instants->Clear(); }

};